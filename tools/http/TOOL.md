---
name: http
description: "Make HTTP requests (GET, POST, PUT, PATCH, DELETE)"
core: false
category: network
---

## Zweck
Ausgehende HTTP-Requests gegen beliebige URLs oder `baseUrl` + `path`.

## Optionales Tool
Standardmäßig deaktiviert. In den Settings unter "Tools" aktivieren, wenn der
Agent externe APIs aufrufen soll.
