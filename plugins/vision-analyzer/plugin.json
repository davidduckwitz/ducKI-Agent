{
  "name": "vision-analyzer",
  "version": "0.2.0",
  "description": "Realtime browser vision for DucKI with zero-dependency local mode, optional click-to-install OCR/ONNX packs and opt-in agent vision.",
  "author": "DucKI",
  "license": "MIT",
  "icon": "👁️",
  "category": "workspace",
  "trust": "node",
  "enabled": true,
  "storage": { "sqlite": true },
  "provides": {
    "moduleTools": ["tools/vision.js"],
    "skills": ["skills/vision"],
    "frontendPage": "frontend/index.html",
    "widgets": [
      {
        "id": "vision-status",
        "title": "Vision",
        "page": "widgets/status/index.html",
        "placement": "sidebar-content",
        "align": "full",
        "frame": "card",
        "background": "card",
        "height": 120,
        "width": "full"
      }
    ],
    "settings": [
      { "key": "VISION_LOCAL_ONLY", "type": "boolean", "default": true, "description": "Local-only mode: never call an LLM for vision. Zero-dependency QR/motion and installed local packs remain available." },
      { "key": "VISION_AUTO_ANALYZE", "type": "boolean", "default": false, "description": "Run LLM vision periodically while a stream is active. Ignored while local-only mode is enabled." },
      { "key": "VISION_ANALYZE_INTERVAL_MS", "type": "number", "default": 5000, "description": "Minimum interval between automatic LLM vision analyses." }
    ]
  }
}
