{
  "name": "@ducki/tauri-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:prep": "node build.js",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "dist": "pnpm -w build:server && pnpm build:prep && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "tsx": "^4.23.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^22.0.0",
    "pkg": "^5.8.1",
    "typescript": "^5.5.0",
    "vite": "^5.0.0"
  }
}
