{
  "name": "@ducki/tauri-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:prep": "node build.js",
    "dist": "pnpm -w build:web && pnpm build:prep && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
