# Time One-Pager

A simple, clean one-pager that displays the actual current time using JavaScript.

## How to use
1. Open `index.html` in any modern web browser.
2. The time will update every second.